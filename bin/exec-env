#!/bin/bash
if [[ $ASDF_INSTALL_VERSION == *"1.11"* ]]; then
  export KUBECONFIG=$HOME/.kube/config.eks
fi
if [[ $ASDF_INSTALL_VERSION == *"1.9.11"* ]]; then
  export KUBECONFIG=$HOME/.kube/config.mini
fi
